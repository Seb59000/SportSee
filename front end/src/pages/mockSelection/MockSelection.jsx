import Header from '../../components/header/Header'
import UserSelection from '../../components/userSelection/UserSelection'

function MockSelection() {
    return (
        <div>
            < Header />
            <main>
                < UserSelection cas="mock" />
            </main>
        </div>
    );
}

export default MockSelection;
